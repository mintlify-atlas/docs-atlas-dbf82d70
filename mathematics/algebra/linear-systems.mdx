---
title: Linear Systems as Matrices
description: Learn to represent systems of linear equations as matrices and solve them using NumPy's powerful linear algebra functions.
---

## Introduction

A **system of linear equations** (or **linear system**) is a collection of one or more linear equations involving the same variables. By representing these systems as matrices, you can leverage NumPy's efficient computational tools to find solutions quickly.

<Info>
This guide demonstrates how to model systems of linear equations as matrices, solve them using NumPy, and understand the relationship between matrix properties and solution existence.
</Info>

## Setup

Load the required packages:

```python
import numpy as np
import matplotlib.pyplot as plt
```

## Understanding Linear Systems

### What is a Linear System?

Consider this example system:

$$
\begin{cases} 
-x_1+3x_2=7, \\ 3x_1+2x_2=1, \end{cases}
$$

This system has:
- Two equations
- Two unknown variables: $x_1$ and $x_2$
- A solution is values for $x_1$ and $x_2$ that satisfy both equations simultaneously

<Note>
A linear system is **singular** if it has no unique solution (either no solution or infinitely many solutions). Otherwise, it is **non-singular**.
</Note>

## Matrix Representation

### Full System Matrix

The complete system can be represented as an **augmented matrix**:

$$
\begin{bmatrix}
-1 & 3 & 7 \\
3 & 2 & 1
\end{bmatrix}
$$

Each row represents one equation. Columns represent:
- Column 1: Coefficients of $x_1$
- Column 2: Coefficients of $x_2$
- Column 3: Constants (right side of equations)

### Coefficient Matrix and Vector

We can separate the system into:

<Tabs>
  <Tab title="Coefficient Matrix A">
    $$
    A = \begin{bmatrix}
    -1 & 3\\
    3 & 2
    \end{bmatrix}
    $$
  </Tab>
  <Tab title="Constant Vector b">
    $$
    b = \begin{bmatrix}
    7 \\
    1
    \end{bmatrix}
    $$
  </Tab>
</Tabs>

### Creating Matrices in NumPy

```python
# Coefficient matrix
A = np.array([
    [-1, 3],
    [3, 2]
], dtype=np.dtype(float))

# Constant vector
b = np.array([7, 1], dtype=np.dtype(float))

print("Matrix A:")
print(A)
print("\nArray b:")
print(b)
```

### Checking Dimensions

```python
print(f"Shape of A: {A.shape}")  # Output: (2, 2)
print(f"Shape of b: {b.shape}")  # Output: (2,)
```

<Tip>
Use the `.shape` attribute or `np.shape()` function to verify matrix dimensions before performing operations.
</Tip>

## Solving Linear Systems

### Using `np.linalg.solve()`

NumPy's linear algebra package provides a fast, reliable solver:

```python
# Solve the system
x = np.linalg.solve(A, b)

print(f"Solution: {x}")
# Output: [-1.  2.]
```

This tells us that $x_1 = -1$ and $x_2 = 2$.

<Steps>
  <Step title="Define Coefficient Matrix">
    Create matrix $A$ with equation coefficients
  </Step>
  <Step title="Define Constant Vector">
    Create vector $b$ with right-hand side values
  </Step>
  <Step title="Call np.linalg.solve()">
    Pass $A$ and $b$ to get solution vector $x$
  </Step>
  <Step title="Verify Solution">
    Substitute values back into original equations
  </Step>
</Steps>

### Verification

Substitute $x_1 = -1$ and $x_2 = 2$ into the original equations:

- Equation 1: $-(-1) + 3(2) = 1 + 6 = 7$ ✓
- Equation 2: $3(-1) + 2(2) = -3 + 4 = 1$ ✓

## Matrix Determinants

### Understanding Determinants

For square matrices, the **determinant** is a scalar value that characterizes matrix properties.

<Info>
A linear system has **one unique solution** if and only if the coefficient matrix has a **non-zero determinant**.
</Info>

### Calculating Determinants

```python
d = np.linalg.det(A)

print(f"Determinant of matrix A: {d:.2f}")
# Output: Determinant of matrix A: -11.00
```

Since $d \neq 0$, the system has a unique solution.

## Visualizing Linear Systems

### Creating the Augmented Matrix

```python
# Horizontally stack A and b
A_system = np.hstack((A, b.reshape((2, 1))))

print(A_system)
# Output:
# [[-1.  3.  7.]
#  [ 3.  2.  1.]]
```

<Note>
Use `.reshape((2, 1))` to convert the 1-D vector into a 2-D column vector before stacking.
</Note>

### Extracting Rows

```python
# Extract second row (indexing starts at 0)
print(A_system[1])
# Output: [3. 2. 1.]
```

### Plotting the Solution

Each equation represents a line in 2D space. The solution is their intersection point:

```python
# Assuming you have a plot_lines function
plot_lines(A_system)
```

The lines intersect at $(x_1, x_2) = (-1, 2)$, confirming our solution.

## Systems with No Solutions

### Example: Parallel Lines

Consider the system:

$$
\begin{cases} 
-x_1+3x_2=7, \\ 3x_1-9x_2=1, \end{cases}
$$

```python
A_2 = np.array([
    [-1, 3],
    [3, -9]
], dtype=np.dtype(float))

b_2 = np.array([7, 1], dtype=np.dtype(float))

# Check determinant
d_2 = np.linalg.det(A_2)
print(f"Determinant of matrix A_2: {d_2:.2f}")
# Output: Determinant of matrix A_2: 0.00
```

<Warning>
When the determinant equals zero, the system is **singular** and has either no solution or infinitely many solutions.
</Warning>

### Attempting to Solve

```python
try:
    x_2 = np.linalg.solve(A_2, b_2)
except np.linalg.LinAlgError as err:
    print(err)
    # Output: Singular matrix
```

### Visualization

```python
A_2_system = np.hstack((A_2, b_2.reshape((2, 1))))
plot_lines(A_2_system)
```

The lines are parallel and never intersect - no solution exists.

## Systems with Infinite Solutions

### Example: Coincident Lines

Modify the system to make the equations proportional:

$$
\begin{cases} 
-x_1+3x_2=7, \\ 3x_1-9x_2=-21, \end{cases}
$$

```python
b_3 = np.array([7, -21], dtype=np.dtype(float))
A_3_system = np.hstack((A_2, b_3.reshape((2, 1))))

print(A_3_system)
# Output:
# [[ -1.   3.   7.]
#  [  3.  -9. -21.]]
```

### Understanding the Solution

The second equation is just $-3$ times the first equation. This reduces to:

$$
\begin{cases} 
-x_1+3x_2=7, \\ 0=0, \end{cases}
$$

The solution is:

$$
x_1=3x_2-7
$$

where $x_2$ can be any real number.

<Info>
When equations are proportional, they represent the same line. Every point on this line is a solution, resulting in **infinitely many solutions**.
</Info>

### Plotting

```python
plot_lines(A_3_system)
```

You'll see only one line because both equations represent the same line.

## Summary Table

| Determinant | System Type | Number of Solutions | Geometric Interpretation |
|-------------|-------------|---------------------|-------------------------|
| $d \neq 0$ | Non-singular | One unique solution | Lines intersect at one point |
| $d = 0$ | Singular | Zero or infinite | Lines are parallel or coincident |

## Key Takeaways

<AccordionGroup>
  <Accordion title="Matrix Representation">
    - Systems can be represented as coefficient matrix $A$ and constant vector $b$
    - Augmented matrix combines both: $[A | b]$
    - Each row is an equation, each column is a variable's coefficients
  </Accordion>

  <Accordion title="Solving Systems">
    - Use `np.linalg.solve(A, b)` for systems with unique solutions
    - Always check the determinant first
    - Verify solutions by substitution
  </Accordion>

  <Accordion title="Determinant Significance">
    - Non-zero determinant: unique solution exists
    - Zero determinant: no solution or infinitely many solutions
    - Calculate with `np.linalg.det(A)`
  </Accordion>

  <Accordion title="Geometric Interpretation">
    - Each equation is a line (in 2D) or hyperplane (in higher dimensions)
    - Solution is the intersection of all lines/planes
    - Parallel lines = no solution
    - Coincident lines = infinite solutions
  </Accordion>
</AccordionGroup>

<Card title="Next: Matrix Operations" icon="forward" href="/mathematics/algebra/matrix-operations">
  Master matrix multiplication and essential operations for linear algebra
</Card>
